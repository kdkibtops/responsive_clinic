export type Clinic = {
    id?: string,
    date: string, //yyyy-mm-dd
    attendingPhysician: number
}

export class products {
    async createNewClinic(date: string, physician: string): Promise<void> {

    }
}